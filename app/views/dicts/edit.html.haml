%h1 Editing dict
- form_for(@dict) do |f|
  = f.error_messages
  %p= f.label :name
  = f.text_field :name
  %p= f.label :description
  = f.text_field :description
  %p= f.submit "Update"
= link_to 'Show', @dict
= link_to 'Back', dicts_path
