%h1 New dict
- form_for(@dict) do |f|
  = f.error_messages
  %p= f.label :name
  = f.text_field :name
  %p= f.label :description
  = f.text_field :description
  %p= f.submit "Create"
= link_to 'Back', dicts_path
